# Loan-Management-System
The Loan Management System is a C-based console program that manages house and car loan applications. It checks eligibility, calculates EMI and total payable amount, stores client records using structures, and generates application forms with unique application numbers.
